import 'package:flutter/material.dart';

/// Shared visual theme so all three apps look consistent.
/// Built in Phase 1.
class AppTheme {
  static ThemeData light = ThemeData(
    useMaterial3: true,
    colorSchemeSeed: const Color(0xFF1F3864),
  );
}
