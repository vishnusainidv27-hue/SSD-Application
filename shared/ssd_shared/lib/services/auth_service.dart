/// Wraps Firebase Authentication for all three apps.
/// Built in Phase 1 – Foundation & Login.
class AuthService {
  // TODO (Phase 1): sign in with mobile + password (or phone OTP – decision pending,
  // see Requirements §2 note and the open question raised about Spark-plan login).
  // TODO (Phase 1): sign out, current-user stream, role lookup from `users` collection.
}
