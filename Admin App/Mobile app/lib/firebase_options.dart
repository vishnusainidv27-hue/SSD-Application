import 'package:firebase_core/firebase_core.dart';

/// PLACEHOLDER — replaced by the real file when you run `flutterfire configure`
/// from this app's folder. Until then, [currentPlatform] throws so main.dart
/// shows the "Firebase isn't configured yet" screen instead of crashing.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase options have not been generated. Run `flutterfire configure`.',
    );
  }
}
